services:

  construction: &construction
    build:
      context: ..
      dockerfile: Dockerfile
      target: ${TARGET}
      args:
        IMAGE: ${IMAGE}
        REPOSITORY: ${REPOSITORY}
    image: ${REGISTRY_IMAGE}

  update_latest:
    image: ${REGISTRY_IMAGE_LATEST}
    <<: *construction
